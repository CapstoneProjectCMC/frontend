import { NgModule } from '@angular/core';
import { StudentStatisticRoutingModule } from './student-statistic-routing.module';

@NgModule({
  imports: [StudentStatisticRoutingModule],
})
export class StudentStatisticModule {}
